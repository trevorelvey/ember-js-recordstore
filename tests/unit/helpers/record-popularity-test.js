import { recordPopularity } from 'record-shop/helpers/record-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | record popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = recordPopularity([42]);
  assert.ok(result);
});
